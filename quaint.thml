export const RAWATOR = {
    id: "ATOM-0",
    state: "GANZES",
    class: "URSPRUNG",

    // Die erste 0: kann alles werden, wenn sie verbunden wird
    connect(target) {
        if (!target || typeof target.activate !== "function") return false;
        return target.activate("0");
    }
};
